# Redis Default Credentials
# Format: username:password (Redis 6+ supports ACL usernames)
# Note: Many Redis instances have no password (empty password = valid if no auth configured)
default:
default:redis
default:password
# --- cautious ---
admin:admin
root:root
redis:redis
# --- aggressive ---
redis:password
redis:admin
default:admin
cache:cache
redis:123456
